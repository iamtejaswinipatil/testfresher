pipeline {
    agent any

    stages {
        stage('clean') {
            steps {
                echo 'clean full'
            }
        }
        stage('validate') {
            steps {
                echo 'validate full'
            }
        }
            stage('compile') {
            steps {
                echo 'compile full'
            }
        }
    }
}
